[project]
name = "spcon"
authors = [
    { name = "Sanpra", email = "21pradipjadhav@gmail.com"}
]
description = "spcon"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    "geopy==2.3.0",
"Shapely==2.1.0"
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

# These dependencies are only installed when developer mode is enabled
[tool.bench.dev-dependencies]
# package_name = "~=1.1.0"
